<ForgotPasswordScreen>:
    BoxLayout:
        id: main
        orientation: 'vertical'

        canvas.before:
            Color:
                rgba: (1, 1, 1, 1)
            Rectangle:
                size: self.size
                pos: self.pos

        BoxLayout:
            orientation: 'vertical'
            size_hint_y: None
            height: 35

            canvas.before:
                Color:
                    rgba: (.1, .1, .1, 1)
                Rectangle:
                    size: self.size
                    pos: self.pos

            Label:
                text: 'Atlas Terminal'
                font_size: 14
                bold: True

        AnchorLayout:
            anchor_x: 'center'
            anchor_y: 'center'

            BoxLayout:
                id: ids
                orientation: 'vertical'
                size_hint_y: None
                padding: self.size[0] / 2.5, 10
                spacing: 10
                height: 230

                Label:
                    text: 'Enviaremos um link para recuperação da senha'
                    font_size: 18
                    bold: True
                    color: (21 / 255, .15, 81 / 255, 1)

                Label:
                    id: info
                    bold: True
                    color: (200 / 255, .1, .1, 1)

                TextInput:
                    id: recovery_email
                    hint_text: 'Email'
                    multiline: False
                    on_text_validate: root.send_recovery_email()

                Button:
                    text: 'Enviar'
                    bold: True
                    on_release: root.send_recovery_email()
                    background_color: (21 / 255, .15, 81 / 255, 1)
                    background_normal: ''

                Button:
                    text: 'Voltar'
                    on_release: root.manager.current = 'login'
                    on_release: root.ids.info.text = ''
                    on_release: root.ids.recovery_email.text = ''
                    background_color: (21 / 255, .15, 81 / 255, 1)
                    background_normal: ''


        BoxLayout:
            id: temp

